# Prism Hardening Wave 9 Validation

## Scope

This report validates the Wave 9 parser, filesystem-admission, source-identity, ceremony-publication, evidence-schema, and canonical-lane changes layered on the Wave 8 hardened tree.

Validated implementation/documentation commit before this report:

- commit: `d3821138de7513097b12ae34434846b3276ceee8`
- tree: `760952a1d0ab3a3fead19803a2a1dcac72f8a4a1`
- Wave 9 patches before this validation report: 18

## Reproducible static admission

The committed receipt regenerated byte-for-byte:

```bash
python3 scripts/verify-wave9-static.py \
  --check security/wave9-static-evidence.json
```

Result: **pass**

The receipt checks:

- recursive duplicate-key rejection in Rust and Python;
- strict UTF-8 and trailing-data handling;
- exact signed/trust schemas through `deny_unknown_fields`;
- pre/post metadata fingerprints for ceremony files, build evidence, reviewer trust files, and Git executables;
- absolute normalized non-overlapping ceremony paths;
- admitted and remeasured Git execution with cleared configuration;
- bounded reviewer policies, ledgers, signatures, rationale, evidence, and policy histories;
- create-once ceremony publication and parent-directory admission;
- exact Rust/Python build-evidence schemas and file sets;
- the active `wave9-static` Rust, Nix, CI, release, and live-gate lane.

Wave 8's historical verifier was not modified. Wave 9 records the exact nine Wave 8 checks superseded by the active-lane promotion and the new `SourceIdentity` implementation. Any other inherited failure remains fatal.

## Supply-chain admission

The committed supply-chain receipt regenerated byte-for-byte:

```bash
python3 scripts/verify-supply-chain.py \
  --check security/supply-chain-evidence.json
```

Result: **pass**

Recorded state:

- 20 workspace crates;
- 25 local packages;
- 813 registry packages;
- 838 total `Cargo.lock` packages;
- no Git dependencies;
- no unknown registries;
- no missing registry checksums;
- no wildcard dependency versions;
- no repository-escaping path dependencies;
- immutable GitHub Action references.

Release-mode admission was also exercised:

```bash
python3 scripts/verify-supply-chain.py --release
```

Result: **expected fail**, with exactly one failure:

> flake.lock has not been generated, committed, semantically reviewed, and hybrid-signed on a Nix-enabled host

No additional release blocker was reported.

## Parser and manifest validation

The available structural parsers reported:

- 88 Rust files parsed without syntax-tree errors;
- 29 TOML files parsed;
- 36 JSON files parsed;
- 13 Python scripts compiled;
- one workflow YAML parsed.

The Rust tree contains 370 `#[test]`, `#[tokio::test]`, and property-test annotations.

A direct Python regression check confirmed that `scripts/prism_strict_json.py`:

- accepts an ordinary object;
- rejects duplicate top-level keys;
- rejects duplicate nested keys.

## Repository integrity

The following checks passed:

```bash
git diff --check
git fsck --full
```

The Wave 9 change set before this report contained:

- 33 changed files;
- 2,491 insertions;
- 217 deletions.

## Manual semantic review

Because no Rust compiler was available, the modified interfaces were also inspected manually for cross-module consistency.

The review covered:

- `SourceIdentity` use in all four release and lock-review preparation/verification binaries;
- strict-parser error propagation through binaries returning `Box<dyn Error>`;
- exact build-evidence object and tool-set agreement between Rust and Python;
- Unix/non-Unix metadata-fingerprint implementations;
- create-once ceremony output semantics and final link-count verification;
- reviewer-policy and policy-history bounds;
- canonical Wave 9 command labels in build receipts, Nix, release verification, and the live gate.

No additional source inconsistency was found during that review.

## Patch-series replay

The ordered mail-patch series was replayed in two independent lanes after the implementation and validation report were complete:

1. the 19-patch Wave 9 delta onto the untouched Wave 8 bundle;
2. the complete 105-patch Waves 1–9 campaign onto the imported baseline commit.

Both `git am` runs completed without conflict, left clean worktrees, passed `git diff --check` and `git fsck --full`, and reproduced the same final source tree. The final tree identity is recorded in the packaged `APPLY.md` files and checksum manifest.

## Gates not executed

This environment did not contain:

- `cargo`;
- `rustc`;
- Nix;
- Bubblewrap.

Therefore this report does **not** claim that:

- the Rust workspace compiled;
- formatting or Clippy passed;
- Rust tests executed;
- the Nix flake evaluated;
- the namespace and egress broker executed;
- production verifier-agent Ed25519 or ML-DSA checks ran;
- a hybrid-signed lock review or release ceremony completed.

`/usr/bin/chromium` was present, but it was not launched because the required Bubblewrap/Nix and release evidence prerequisites were absent.

## Canonical next validation

On a Nix-enabled host with the pinned Rust toolchain and production verifier agent:

1. generate and commit `flake.lock`;
2. run semantic lock admission;
3. complete the offline hybrid lock-review ceremony;
4. capture frozen Rust evidence;
5. capture Nix evidence with `--no-update-lock-file`;
6. build authenticated-reviewed indexes;
7. prepare and assemble the hybrid release attestation;
8. execute `scripts/run-wave9-live-gates.py` with all five evidence inputs;
9. independently verify the final release.

Until those steps pass, Prism remains statically admitted but not release-admitted.
