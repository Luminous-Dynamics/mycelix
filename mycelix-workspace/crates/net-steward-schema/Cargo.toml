[package]
name = "net-steward-schema"
version = "0.1.0"
edition = "2021"
authors = ["Luminous Dynamics Core Team"]
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ed25519-dalek = "2.1"
hex = "0.4"
