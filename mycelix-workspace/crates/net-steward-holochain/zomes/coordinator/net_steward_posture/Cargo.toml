[package]
name = "net_steward_posture_coordinator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = "0.4.0"
serde = { version = "1.0", features = ["derive"] }
net_steward_posture_integrity = { path = "../../integrity/net_steward_posture" }
net-steward-schema = { path = "../../../../net-steward-schema" }
