[package]
name = "net-steward-holochain"
version = "0.1.0"
edition = "2021"
authors = ["Luminous Dynamics Core Team"]
license = "MIT OR Apache-2.0"

[dependencies]
net-steward-schema = { path = "../net-steward-schema" }
net-steward-discovery = { path = "../net-steward-discovery" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tungstenite = "0.20"

[features]
default = []
holochain-conductor-tests = []
