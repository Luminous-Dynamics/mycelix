# GOVSYS-003C-A Constitutional Rooted-Lineage Composition v0.1

Status: **qualification-first one-edge composition theorem**

This tranche is the first semantic child of exact hosted-qualified Root-C convergence PR #845.

It proves one exact end-to-end bridge:

```text
externally provisioned Root-B pin
        +
raw GOVSYS-003C-V predecessor/successor/material/proof bundle
        ↓
re-execute exact Root-B + predecessor-owned transition qualification
        ↓
derive constitutional lineage-domain identity
        ↓
project exact structural facts into CORE-LINEAGE Stage 1
        ↓
stable constitutional history identity
        +
separate deployment qualification-provenance identity
```

The theorem is deliberately qualification-first. It establishes semantic composition and
cross-language identity agreement. It does **not** claim that a serialized Python result is
a production Rust authority token. Production positive-token origin remains #847 + #848.

## Exact parent

Parent:

`c9d1d744919f1b0e0190a3122b6708aee9e70a89`

Hosted parent qualification:

`34883262617` — **PASS**

The parent already proves exact assembly/executable compatibility of:

- Root-B pinned provenance;
- CORE-LINEAGE Stage 1; and
- GOVSYS-003C-V predecessor-owned transition verification.

This child adds the missing semantic projection without changing any inherited theorem blob.

## Central separation

```text
verified predecessor-authorized edge
!= rooted constitutional history

rooted history over supplied verified evidence
!= complete global constitutional history

stable constitutional history
!= deployment qualification provenance

historical endpoint
!= current constitutional root
```

Root-D owns closed-world coverage and current-head promotion.

## Lineage-domain identity

Profile:

`mycelix-constitutional-root-lineage-domain-v1-sha256-framed-semantic`

Domain separator:

`mycelix/public-institution/constitutional-root-lineage-domain/v1`

The canonical transcript is:

```text
DOMAIN_UNFRAMED
frame(lineage-domain profile)
frame(Root-A identity profile)
frame(Root-A protocol_version)
frame(institution_id)
optional-frame(jurisdiction_id)
frame(bootstrap_mode)
frame(bootstrap_profile)
frame(authoritative_root_source_ref)
frame(root_coverage_profile)
frame(root_source_verification_profile)
```

This binds exactly the semantic fields that ordinary
`constitutional-root-rotation-v1` must preserve.

It deliberately excludes:

- constitutional rulebook identity/version/content;
- authorized ordinary policy scopes;
- validity timestamps;
- source-verification anchor;
- rotation mode/profile/anchor; and
- Root-B provisioning metadata.

Those exclusions are intentional. The frozen #839 successor rotates its source anchor,
future rotation authority and constitutional rulebook while preserving the same lineage domain.

For the aligned #839 predecessor/successor fixture the exact domain digest is:

`6067a895dfec8c8a6f5650e5f2031cf981c116f5b22fc1e5f316893f6dcb7f7a`

A change to any domain-bound field is migration/recovery, not ordinary rotation.

## Root-B bridge fixture

The previously qualified Root-B golden example and the #839 cryptographic transition example
use different valid generation-zero roots. This tranche does not silently mix them.

It supplies a second **input pin** to the unchanged Root-B theorem for the exact #839 predecessor:

```text
root digest
b7a0c7cb28f182d06367d4bf1d3cc4f7d82094701d955eeaa7ec53153d2080a6

provisioning ref
deployment:example-city:constitutional-root-pin:transition-fixture:v1
```

Exact resulting Root-B provenance identity:

`6d4a106b254a5c6ef7845aca919a729353063c15860eb2a9a9ce24aac068c6ac`

This is not a new bootstrap profile or Root-B theorem.

## Exact one-edge structural projection

After Root-B and #839 are re-executed successfully, structural facts are locally derived.

Genesis/root anchor:

```text
lineage domain
  = exact derived domain identity

generation
  = 0

node identity
  = exact independently recomputed predecessor Root-A identity

source descriptor
  = exact independently recomputed predecessor source-descriptor identity

effective_at_ms
  = predecessor Root-A valid_from_ms
```

Transition facts:

```text
predecessor generation/node/source
  = exact verified predecessor facts

successor generation/node/source
  = exact verified successor facts

transition semantic identity
  = exact #839 canonical signed-candidate identity

effective_at_ms
  = exact verified transition effective time
```

No caller-supplied generic `TransitionFacts` are qualification authority.

## Exact CORE-LINEAGE agreement

The qualification oracle independently reproduces the frozen
`mycelix-core-lineage-v1-sha256-framed-semantic` encoding.

A separate checked-in Rust harness then calls the **actual inherited CORE-LINEAGE crate**
with the same structural facts and must obtain the same digest.

Exact one-edge CORE-LINEAGE commitment:

`e039378be03e597f6d3c117890e8395cb112efbe6f19e2c7e0a2ccdf37eb1b74`

This proves cross-language agreement for the bridge fixture rather than merely running
two unrelated test suites.

## Replay-key theorem

Before structural projection, replay state is keyed as:

```text
(
  predecessor Root-A identity,
  exact replay nonce bytes
)
    -> stable transition semantic identity
```

Rules:

```text
same replay key + same stable transition identity
    -> exact semantic duplicate; harmless

same replay key + different stable transition identity
    -> ReplayNonceConflict
```

The qualification oracle routes this check through the exact inherited #839 replay helper.

This one-edge tranche does not claim global replay closure over evidence it never received.
The later multi-edge child expands the corpus without changing this rule.

## Stable history identity

Profile:

`mycelix-constitutional-root-lineage-v1-sha256-framed-semantic`

Domain separator:

`mycelix/public-institution/constitutional-root-lineage/v1`

Commitment:

```text
DOMAIN_UNFRAMED
frame(profile)
profiled(lineage-domain identity)
profiled(exact CORE-LINEAGE stable commitment)
```

Exact aligned-fixture identity:

`2501ddf71646e27797822390310f0c558a72491f8ec46e0efdd3fd4647c09317`

This identity intentionally excludes Root-B provisioning metadata and dynamic verifier evidence.

## Qualification provenance identity

Profile:

`mycelix-constitutional-root-lineage-qualification-v1-sha256-framed-semantic`

Domain separator:

`mycelix/public-institution/constitutional-root-lineage-qualification/v1`

Commitment:

```text
DOMAIN_UNFRAMED
frame(profile)
profiled(exact Root-B provenance identity)
profiled(exact stable constitutional history identity)
```

For the primary bridge pin:

`cf1ae9c79767ed5d4da31448cb9a4b641d797f495a664d50f143839d917fde9d`

For an alternate provisioning reference that pins the exact same Root-A/history:

```text
Root-B provenance
6debe1913282a5ddd7c440798ca2697ad3702e2f301e0614402bfc4c7ca4acd7

qualification provenance
9d2d572c26be9dced64fc1eb644dfc2a4c8de8ec15ac5900f3a356c215e26437
```

The stable constitutional-history identity remains exactly unchanged.

## Evidence origin

Positive qualification starts from raw inputs and **re-executes**:

1. exact Root-A semantic validation;
2. exact Root-B external pin qualification;
3. exact #839 Root-A predecessor/successor rebinding;
4. exact predecessor rotation-authority material commitment;
5. exact Ed25519 authorization verification; and
6. exact replay-key semantics.

A deserialized dictionary claiming `verified=true`, a stored positive receipt, a caller-selected
key, or caller-provided root/source/transition digest is insufficient.

The qualification oracle outputs evidence for review/testing only. It does not manufacture a
production capability.

## Observed-history boundary

A successful projection means:

> the exact verified evidence supplied to this qualifier forms this rooted,
> internally conflict-free constitutional history.

It does **not** mean:

> no other valid transition exists outside the evidence supplied here.

Absence/completeness is a closed-world source theorem and remains Root-D/#842.

## Hosted qualification requirements

The dedicated gate must establish:

- exact parent `c9d1d744...`;
- exactly one child commit and exactly five new files;
- no inherited theorem blob changed;
- Root-A, Root-B and #839 re-execute successfully;
- new Root-C-A oracle passes;
- OpenSSL independently verifies the frozen #839 Ed25519 fixture;
- exact domain/history/provenance golden identities;
- actual inherited CORE-LINEAGE Rust crate returns the exact same one-edge commitment;
- exact duplicate structural transition remains harmless;
- same P/S with another stable transition identity produces `ParallelTransitionConflict`;
- replay-key collision with another stable transition identity fails closed before structural reinterpretation;
- domain-bound migration is rejected by ordinary #839 rotation;
- deployment-ref substitution changes qualification provenance but not stable history;
- Rust harness formatting/Clippy succeeds;
- immutable checkout; and
- final fail-closed aggregation.

## Nonclaims

This theorem grants no:

- globally complete constitutional transition set;
- globally fork-free history;
- current constitutional root;
- closed-world root-source coverage;
- ordinary policy currentness;
- legal or democratic legitimacy;
- administrative/judicial authority;
- execution authority;
- coercive authority; or
- external-effect authority.

`grants_currentness = false`

`grants_effect_authority = false`

The next semantic expansion should add the already-designed two-edge cryptographic fixture and
terminal immutable successor as a separate evidence child. Production promotion should later
converge exact-qualified #847 and #848 without changing these stable Root-C semantics.

The network remains infrastructure for institutions. It is not the sovereign.
