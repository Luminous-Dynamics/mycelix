[package]
name = "pulse-realtime-types"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Transport-neutral, non-authoritative realtime hint contracts for Mycelix Pulse"

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
