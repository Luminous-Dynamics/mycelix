[build]
target = "apps/marketplace-web/index.html"
dist = "dist"
public_url = "/"

[serve]
port = 8080
open = false

[[hooks]]
stage = "pre_build"
command = "npm"
command_arguments = ["--prefix", "bridge", "run", "build"]
