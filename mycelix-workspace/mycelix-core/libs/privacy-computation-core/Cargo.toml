[package]
name = "privacy-computation-core"
version = "0.1.0"
edition = "2024"
authors = ["Mycelix Network"]
description = "Authority-neutral semantic types for privacy-enhancing computation composition"
license = "AGPL-3.0-or-later"
repository = "https://github.com/Luminous-Dynamics/mycelix"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
