[package]
name = "mycelix-fhe-core"
version = "0.1.0"
edition = "2024"
authors = ["Mycelix Network"]
description = "Authority-neutral homomorphic-encryption semantics for Mycelix PEC"
license = "AGPL-3.0-or-later"

[dependencies]
privacy-computation-core = { path = "../privacy-computation-core" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
