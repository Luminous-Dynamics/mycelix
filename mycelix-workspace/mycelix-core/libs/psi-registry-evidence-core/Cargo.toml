[package]
name = "psi-registry-evidence-core"
version = "0.1.0"
edition = "2024"
authors = ["Mycelix Network"]
description = "Provider-neutral registry subject and structural evidence semantics for PSI contact discovery"
license = "AGPL-3.0-or-later"
repository = "https://github.com/Luminous-Dynamics/mycelix"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1.0"
