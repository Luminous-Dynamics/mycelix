[package]
name = "psi-abuse-control-profiles"
version = "0.1.0"
edition = "2024"
authors = ["Mycelix Network"]
description = "Authority-neutral abuse-control composition profiles for Mycelix PSI"
license = "AGPL-3.0-or-later"
repository = "https://github.com/Luminous-Dynamics/mycelix"
publish = false

[dependencies]
privacy-computation-core = { path = "../privacy-computation-core" }
privacy-protocol-profiles = { path = "../privacy-protocol-profiles" }
serde = { version = "1.0", features = ["derive"] }
