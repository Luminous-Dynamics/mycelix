[package]
name = "mycelix-privacy-leakage-core"
version = "0.1.0"
edition = "2024"
authors = ["Mycelix Network"]
description = "Authority-neutral privacy leakage observation evidence for Mycelix PEC"
license = "AGPL-3.0-or-later"

[dependencies]
privacy-computation-core = { path = "../privacy-computation-core" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
