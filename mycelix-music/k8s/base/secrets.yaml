apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  labels:
    app.kubernetes.io/name: mycelix-api
type: Opaque
stringData:
  # These are placeholder values - replace in production overlay or use sealed-secrets
  database-url: "postgresql://mycelix:changeme@postgres:5432/mycelix_music"
  redis-url: "redis://redis:6379"
