[package]
name = "mycelix-authority-coordinator-deployment"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure exact-match contract for security-critical Holochain coordinator deployment evidence"

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
