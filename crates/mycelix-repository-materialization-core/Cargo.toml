[package]
name = "mycelix-repository-materialization-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false
description = "Pure semantic publication and recovery protocol for bounded repository materialization"

[lib]
name = "mycelix_repository_materialization_core"
path = "src/lib.rs"

[dependencies]
