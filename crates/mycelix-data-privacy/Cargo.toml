[package]
name = "mycelix-data-privacy"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure reviewed privacy/purpose compatibility semantics for embodied datasets"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
