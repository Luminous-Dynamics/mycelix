[package]
name = "mycelix-integration-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Provider-neutral semantic kernel for the Mycelix integration plane"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1.0"
