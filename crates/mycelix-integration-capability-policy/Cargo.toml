[package]
name = "mycelix-integration-capability-policy"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Institution-adopted semantic mapping from integration operations to Mycelix capabilities"

[dependencies]
blake3 = "1.8"
mycelix-institutional-core = { path = "../mycelix-institutional-core" }
mycelix-integration-core = { path = "../mycelix-integration-core" }
serde = { version = "1", features = ["derive"] }
thiserror = "2.0"
