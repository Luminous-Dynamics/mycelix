[package]
name = "mycelix-authority-coordinator-release"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure authenticated release requirement contract for Mycelix coordinator deployment"

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }
mycelix-authority-coordinator-deployment = { path = "../mycelix-authority-coordinator-deployment" }

[dev-dependencies]
serde_json = "1"
