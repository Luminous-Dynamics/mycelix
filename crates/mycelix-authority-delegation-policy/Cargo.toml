[package]
name = "mycelix-authority-delegation-policy"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure institution-bound delegation policy qualification for Mycelix authority grants"

[dependencies]
serde = { version = "1", features = ["derive"] }
blake3 = "1"
mycelix-institutional-core = { path = "../mycelix-institutional-core" }
mycelix-authority-identity = { path = "../mycelix-authority-identity" }
mycelix-authority-freshness = { path = "../mycelix-authority-freshness" }
