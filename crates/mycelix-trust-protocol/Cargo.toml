[package]
name = "mycelix-trust-protocol"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Versioned cross-domain trust-resolution protocol types for Mycelix"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
holochain_serialized_bytes = "=0.0.57"
serde_json = "1.0"
