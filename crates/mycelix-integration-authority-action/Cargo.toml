[package]
name = "mycelix-integration-authority-action"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Deterministic exact-byte governance action bridge for Mycelix integration commands"

[dependencies]
mycelix-execution-action-digest = { path = "../mycelix-execution-action-digest" }
mycelix-governance-authority = { path = "../mycelix-governance-authority" }
mycelix-institutional-core = { path = "../mycelix-institutional-core" }
mycelix-integration-core = { path = "../mycelix-integration-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
