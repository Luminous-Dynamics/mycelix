[package]
name = "mycelix-authority-evidence-lease"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Monotone verifier-evidence lease composition and provenance for Mycelix authority runtimes"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
blake3 = "1"
