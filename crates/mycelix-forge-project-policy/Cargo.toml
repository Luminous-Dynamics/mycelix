[package]
name = "mycelix-forge-project-policy"
version = "0.1.0"
edition = "2021"
rust-version = "1.96"
authors = ["Luminous Dynamics"]
license = "AGPL-3.0-or-later"
repository = "https://github.com/Luminous-Dynamics/mycelix"
description = "Typed project policy state and authentication-provider trust policy for Mycelix Forge"
publish = false

[dependencies]
mycelix-forge-core = { path = "../mycelix-forge-core" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"

[workspace]
