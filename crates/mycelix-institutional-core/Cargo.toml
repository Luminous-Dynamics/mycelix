[package]
name = "mycelix-institutional-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Wire-neutral institutional authority, intent, evidence, decision, challenge, appeal, and remedy primitives for Mycelix"
repository = "https://github.com/Luminous-Dynamics/mycelix"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
