[package]
name = "mycelix-authority-coordinator-conductor-process"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
license = "AGPL-3.0-or-later"
description = "Pinned Linux process/listener stability fence for Mycelix Holochain conductor admission"

[dependencies]
blake3 = "1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
