[package]
name = "mycelix-governance-execution-lifecycle"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Append-only fenced execution lifecycle for binding Mycelix governance"

[dependencies]
serde = { version = "1", features = ["derive"] }
blake3 = "1"
mycelix-institutional-core = { path = "../mycelix-institutional-core" }
