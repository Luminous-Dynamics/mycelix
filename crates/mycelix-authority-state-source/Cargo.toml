[package]
name = "mycelix-authority-state-source"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Append-only generation source and deterministic projection for Mycelix authority freshness"

[dependencies]
serde = { version = "1", features = ["derive"] }
blake3 = "1"
mycelix-authority-freshness = { path = "../mycelix-authority-freshness" }
mycelix-institutional-core = { path = "../mycelix-institutional-core" }
