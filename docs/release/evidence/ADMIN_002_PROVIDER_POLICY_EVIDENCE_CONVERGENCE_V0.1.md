# ADMIN-002 provider-policy evidence convergence v0.1

## Purpose

This tranche converges three independently qualified theorem families without claiming any new policy fact:

1. canonical procedure-policy currentness-provider identity (#815);
2. generic policy record/adoption evidence plus its monotone EvidenceLease parent (#812 / #181); and
3. institutional-core typed-ID semantic-boundary hardening (#823).

The convergence exists so later provider-policy record/adoption composition has real Git ancestry to all three qualified lines and actually executes against the hardened institutional core.

## Exact ancestry

The selective merge has ordered parents:

1. `fc452111cb6071541b7a418347dae8721bac3d80` — qualified #815 provider-policy identity;
2. `00df4a9b39273e4953d02fd5e580ff7f7fe4b0d9` — qualified #812 policy-evidence waist, with qualified #181 in its ancestry;
3. `856788a5a47f6690ebc19b982809495ad997e0c0` — qualified #823 institutional-core typed-ID hardening.

Its tree is `3572fe5b91321bc9ddefd9fb554be856d3e72174` and is constructed from the exact #815 tree plus only the nine product/security blobs named below.

## Imported product surface

From qualified #181:

- `crates/mycelix-authority-evidence-lease/Cargo.toml`
- `crates/mycelix-authority-evidence-lease/INVARIANTS.md`
- `crates/mycelix-authority-evidence-lease/src/lib.rs`

From qualified #812:

- `crates/mycelix-policy-evidence/Cargo.toml`
- `crates/mycelix-policy-evidence/INVARIANTS.md`
- `crates/mycelix-policy-evidence/src/lib.rs`

From qualified #823:

- `crates/mycelix-institutional-core/TYPED_ID_VALIDATION_V0.1.md`
- `crates/mycelix-institutional-core/src/lib.rs`
- `crates/mycelix-institutional-core/tests/authority_identifier_validation.rs`

The institutional-core `Cargo.toml` is byte-identical between #815 and #823, so no manifest replacement is required.

No historical write-capable workflow is imported into the selective merge. The parent commits retain their own qualification history through real Git ancestry.

## Qualification theorem

A hosted PASS establishes only that:

- the ordered three-parent ancestry is exact;
- the selective merge tree is exact;
- the first-parent-to-merge product delta is exactly the nine frozen imported paths;
- every imported blob is byte-identical to its exact qualified source commit;
- the provider-policy identity, policy-evidence waist, EvidenceLease, and institutional-core hardening execute together against one converged tree; and
- the qualification checkout remains immutable.

## Permanent nonclaim

This convergence does **not** prove that any record/adoption evidence exists for the provider-selection policy identified by #815.

Therefore:

`qualified provider-policy identity + qualified evidence waist + hardened typed-ID boundary`

`!= provider-policy record authenticity`

`!= provider-policy institutional adoption`

`!= verifier-origin authenticity`

`!= governing/current provider-selection policy`

`!= procedure-policy currentness`

`!= administrative authority`

`!= external-effect authority`.

The next child may derive the exact #812 `PolicyIdentityRef` from a live `QualifiedCurrentnessProviderPolicyIdentity` and join exact record/adoption evidence. It must remain non-current and non-authoritative, and it must not upgrade #812's transport verifier references into verifier-origin proof.
