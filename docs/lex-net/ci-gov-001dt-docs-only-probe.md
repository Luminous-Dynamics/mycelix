# CI-GOV-001DT docs-only probe

This file exists only to test GitHub's native `pull_request.paths-ignore` admission semantics for the exact `docs/lex-net/**` class.

Probe revision: tree-change-v2.

It grants no CI, product, scientific, merge, or deployment claim.
