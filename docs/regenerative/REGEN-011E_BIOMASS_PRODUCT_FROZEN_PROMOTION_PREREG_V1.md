# REGEN-011E — Biomass ProductFrozen Promotion Preregistration v1

Status: preregistration only. This document freezes how one successful REGEN-011D biomass preparation capsule may become an immutable ProductFrozen subject. It does not itself qualify biomass, reserve material, record physical consumption, operate a process, or authorize any physical action.

## 1. Governing theorem

```text
successful exact biomass preparation
+ exact prepared-byte promotion
+ exact qualified shared-admission dependency
+ ProductFrozen locked execution
+ validated Q001 receipt
= bounded biomass planning-software theorem
```

not:

```text
workflow success
= sustainable biomass
= legal rights
= uncontaminated feedstock
= process execution
= consumed material
= qualified biochar
```

## 2. Required upstream subjects

Promotion is downstream of:

- REGEN-019D / #1315 ProductFrozen shared admission, exact ProductHead `872151cae7bee995b6bfff7867fc40178ad4759e`;
- REGEN-Q001 / #996 machine-readable qualification receipt;
- REGEN-008 / #972 ProductFrozen dependency semantics;
- REGEN-008A / #1280 cross-step toolchain binding;
- REGEN-011A / #1294 consumer firewall;
- REGEN-011B/B1/B2/B3/B4/B5 executable biomass contracts;
- one exact successful REGEN-011D preparation lineage.

REGEN-011C physical-consumption accounting remains a later theorem and MUST NOT be smuggled into the first biomass ProductFrozen subject.

## 3. Preparation admissibility

A promotable preparation run must prove at least:

1. exact authored biomass ProductHead;
2. exact parent `872151ca...`;
3. exact child-owned authored scope;
4. explicit Rust/Cargo/rustfmt 1.96.0 at every material stage;
5. exact machine-generated Cargo.lock;
6. exact formatted biomass source and independent matrix bytes;
7. all biomass matrix tests PASS;
8. strict Clippy PASS;
9. authored checkout restored cleanly;
10. exact preparation artifact archive identity and internal file map.

A failed preparation head remains historical evidence and is not promotable.

The historical `e34366dbe248e1216c93ac115093b6f17d994c84` preparation is explicitly non-promotable because strict Clippy rejected its unboxed `FeedstockAssessment::Eligible(EligibleFeedstock)` representation.

## 4. Exact internal capsule verification

Archive digest alone is insufficient. Promotion must verify the exact expected file set and per-file SHA-256 values for at least:

```text
Cargo.toml
Cargo.lock
src/lib.rs
tests/biomass_core.rs
```

No extra, missing, renamed, or manually repaired product file is accepted silently.

If a product byte needs correction, REGEN-011D must produce a new preparation lineage first.

## 5. Product topology

The ProductFrozen biomass subject should be rebuilt directly on the qualified REGEN-019D ProductHead rather than inheriting preparation/promoter history.

Conceptual product scope:

```text
.github/workflows/regen-biomass-product-frozen.yml
qualification/regen-011e/verify-regen-qualification-receipt.py
crates/mycelix-regenerative-biomass/Cargo.toml
crates/mycelix-regenerative-biomass/Cargo.lock
crates/mycelix-regenerative-biomass/src/lib.rs
crates/mycelix-regenerative-biomass/tests/biomass_core.rs
```

The Q001 validator copy is qualification tooling, not runtime product API.

No REGEN-011D preparation workflow, staging promoter, formatter repair commit, or temporary CI artifact belongs in ProductFrozen ancestry.

## 6. Shared-admission dependency binding

The biomass theorem depends on the exact qualified shared-admission implementation rather than a compatible-looking later version.

Qualification must bind at minimum:

```text
REGEN-019D ProductHead = 872151cae7bee995b6bfff7867fc40178ad4759e
shared admission lib Git blob = 294b777c95be79a13ca7d0e1e3463d32ff25c8f0
shared admission Cargo.lock Git blob = 55ce1ba153982351cce6fd29a2c1174ce40813f1
regenerative core lib Git blob = 97978da98e9c476061b3d3ce1ba605f0c628a1b6
```

and the PEF source anchors already established by REGEN-019D.

```text
shared admission qualified
!= biomass qualified
```

The biomass ProductHead earns its own theorem.

## 7. Dependency semantics

The ProductFrozen ProductHead already contains the promoted Cargo.lock before qualification starts.

Every Cargo resolver/test/lint command uses both:

```text
+1.96.0
--locked
```

The qualifier must not generate, update, or normalize the lock.

Receipt state remains:

```text
dependency_state=ProductFrozen
system_closure=unfrozen
```

unless a stronger system-closure theorem is separately established.

## 8. Qualification campaign

The dedicated exact-head campaign should require, in order:

1. exact ProductHead checkout;
2. exact sole qualified parent and exact changed-path scope;
3. exact preparation-derived manifest/lock/source/matrix identities;
4. exact Q001 validator identity;
5. exact REGEN-019D shared-admission/core/PEF anchors;
6. explicit Rust/Cargo/rustfmt 1.96.0 identity checks;
7. formatting check without mutation;
8. biomass independent matrix under `--locked`;
9. strict all-target Clippy with warnings denied;
10. post-execution source/lock/test/validator immutability;
11. clean checkout;
12. Q001 receipt generation;
13. exact Q001 semantic validation;
14. receipt artifact upload.

Receipt generation occurs only after all product qualification gates have succeeded.

## 9. Behavioral proposition

The first ProductFrozen proposition remains intentionally narrow:

> The exact REGEN-011E biomass ProductHead satisfies the frozen dependency-light biomass planning invariants—exact mass/basis accounting, shared PEF admission consumption, explicit material residuals, ecological-function separation, fail-closed eligibility, assessment-scoped reservation capacity, and pure anti-overbooking arithmetic—under the recorded Rust 1.96.0 ProductFrozen Cargo graph.

It does not establish ecological truth, rights validity, contamination safety, process suitability outside the exact adopted profile references, physical consumption, process execution, output quality, agronomic efficacy, or carbon removal.

## 10. Required corrected-candidate regressions

The promoted candidate must include the refinements learned during REGEN-011D preparation:

- `FeedstockAssessment::Eligible` uses indirection so strict Clippy accepts the enum layout without lint suppression;
- positive ecological eligibility must bind the exact same material-partition reference as the feedstock assessment;
- positive ecological eligibility must bind the exact same evidence-snapshot reference as the feedstock assessment;
- an evidence-snapshot mismatch is an explicit fail-closed error;
- all prior external matrix cases remain green.

## 11. Reservation boundary

The ProductFrozen core may establish only software reservation arithmetic:

```text
eligible assessment
-> scoped ReservationCapacity
-> bounded reservations
```

It must preserve:

```text
reservation
!= physical consumption
!= process execution
```

REGEN-011C remains the separate consumption-accounting theorem before REGEN-012 biochar may interpret process input as consumed material.

## 12. Historical qualifier routing

The dedicated biomass ProductFrozen qualifier should be head-branch-specific while retaining exact SHA/parent/path guards internally. Descendant PRs should see the historical qualifier as not-applicable/skipped rather than intentionally red due to a different parent.

## 13. Deliberate non-claims

Even a successful REGEN-011E campaign establishes no:

- current biomass existence or availability;
- ecological sustainability of any real removal;
- ownership, custody authenticity, harvest/removal/transfer/process right;
- contamination cleanliness or laboratory validity;
- spatial containment, sampling representativeness, or evidence currentness;
- automatic PEF floating-value to exact-milligram normalization;
- process safety, reactor setting, or process execution;
- physical biomass consumption;
- biochar product qualification;
- agronomic suitability or treatment efficacy;
- carbon removal or climate-credit authority;
- marketplace entitlement or economic optimality;
- governance or physical-actuation authority.

Its proposition remains software planning integrity only.
