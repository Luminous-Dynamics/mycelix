# PR1 Scope

This branch introduces only the canonical shared application composition boundary. Governance migration and any visual redesign are explicitly out of scope until the shared API is verified against current `mycelix-leptos-core` contracts.
