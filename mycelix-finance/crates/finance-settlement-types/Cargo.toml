[package]
name = "finance-settlement-types"
version = "0.1.0"
edition = "2024"
description = "Value-class and settlement boundary types for Mycelix Finance"
license = "AGPL-3.0-or-later"

[dependencies]
serde = { workspace = true }
finance-wire-types = { path = "../finance-wire-types" }
