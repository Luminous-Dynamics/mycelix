[package]
name = "finance-collateral-deposit"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Versioned collateral deposit request identity and persistence semantics"

[dependencies]
serde = { workspace = true }
blake3 = "1.8"
finance-collateral-settlement = { path = "../finance-collateral-settlement" }

[dev-dependencies]
serde_json = { workspace = true }
