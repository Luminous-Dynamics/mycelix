[package]
name = "finance-collateral-safety"
version = "0.1.0"
edition = "2024"
description = "Fail-closed collateral health assessment for Mycelix Finance"
license.workspace = true

[lib]
name = "finance_collateral_safety"
crate-type = ["rlib"]

[dependencies]
serde = { workspace = true }
mycelix_finance_types = { path = "../../types" }

[dev-dependencies]
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
