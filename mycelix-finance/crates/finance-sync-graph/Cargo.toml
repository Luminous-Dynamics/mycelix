[package]
name = "mycelix-finance-sync-graph"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Canonical multi-leg settlement graph semantics for Mycelix Finance"

[lib]
crate-type = ["rlib"]
name = "mycelix_finance_sync_graph"

[dependencies]
mycelix-finance-exact = { path = "../finance-exact" }
serde = { workspace = true }
sha2 = "0.10"

[dev-dependencies]
serde_json = { workspace = true }
proptest = "1"
