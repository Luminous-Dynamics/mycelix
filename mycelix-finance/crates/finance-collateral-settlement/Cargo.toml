[package]
name = "finance-collateral-settlement"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Evidence-gated collateral-backed SAP settlement kernel"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
