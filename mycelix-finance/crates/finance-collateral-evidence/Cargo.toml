[package]
name = "finance-collateral-evidence"
version = "0.1.0"
edition = "2024"
description = "Versioned persisted collateral-health evidence for Mycelix Finance"
license.workspace = true

[lib]
name = "finance_collateral_evidence"
crate-type = ["rlib"]

[dependencies]
serde = { workspace = true }
finance-collateral-safety = { path = "../finance-collateral-safety" }
finance-collateral-valuation = { path = "../finance-collateral-valuation" }

[dev-dependencies]
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
