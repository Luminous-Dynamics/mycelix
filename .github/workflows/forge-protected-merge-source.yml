name: Forge Protected Merge Source

on:
  pull_request:
    paths:
      - 'crates/mycelix-forge-protected-merge-source/**'
      - 'crates/mycelix-forge-protected-merge-request/**'
      - 'crates/mycelix-forge-proposal/**'
      - 'crates/mycelix-forge-repository/**'
      - 'crates/mycelix-forge-authority/**'
      - 'crates/mycelix-forge-project-policy/**'
      - '.github/workflows/forge-protected-merge-source.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
        with:
          toolchain: 1.96.0
          components: rustfmt, clippy
      - name: rustfmt
        working-directory: crates/mycelix-forge-protected-merge-source
        run: cargo fmt --all -- --check
      - name: clippy
        working-directory: crates/mycelix-forge-protected-merge-source
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: tests
        working-directory: crates/mycelix-forge-protected-merge-source
        run: cargo test --all-features
