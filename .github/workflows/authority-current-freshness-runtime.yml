name: Authority Current Freshness Runtime

on:
  pull_request:
    paths:
      - 'mycelix-governance/zomes/authority-current-freshness-verifier/**'
      - 'mycelix-governance/Cargo.toml'
      - 'mycelix-governance/dna/dna.yaml'
      - 'crates/mycelix-authority-operational-freshness/**'
      - 'crates/mycelix-authority-operational-context/**'
      - 'crates/mycelix-authority-control-plane-freshness/**'
      - 'crates/mycelix-authority-state-bootstrap-root/**'
      - '.github/workflows/authority-current-freshness-runtime.yml'
  push:
    branches:
      - 'security/authority-current-freshness-provider-split-v0.1'
      - 'security/authority-current-freshness-runtime-contract-v0.1'

permissions:
  contents: read

concurrency:
  group: authority-current-freshness-runtime-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  qualify:
    name: Split-verifier current freshness composition
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown
          components: rustfmt, clippy

      - name: Format
        working-directory: mycelix-governance
        run: cargo fmt --package authority_current_freshness_verifier -- --check

      - name: Native check
        working-directory: mycelix-governance
        run: cargo check --package authority_current_freshness_verifier --all-targets

      - name: Native clippy
        working-directory: mycelix-governance
        run: cargo clippy --package authority_current_freshness_verifier --all-targets -- -D warnings

      - name: WASM check
        working-directory: mycelix-governance
        run: cargo check --release --target wasm32-unknown-unknown --package authority_current_freshness_verifier

      - name: Split-verifier authority audit
        run: |
          python3 - <<'PY'
          from pathlib import Path

          src = Path('mycelix-governance/zomes/authority-current-freshness-verifier/coordinator/src/lib.rs').read_text()
          inv = Path('mycelix-governance/zomes/authority-current-freshness-verifier/INVARIANTS.md').read_text().lower()
          cargo = Path('mycelix-governance/Cargo.toml').read_text()
          dna = Path('mycelix-governance/dna/dna.yaml').read_text()

          source_request = src.split('pub struct SourceHeadVerificationRequest {', 1)[1].split('}', 1)[0]
          assert 'pub probe_action: ActionHash' in source_request
          assert 'VerifiedCoverageChallenge' not in source_request
          assert 'SourceHeadVerificationRequest { probe_action }' in src
          assert 'verify_probe(probe_action.clone(), expected_subject)' in src

          required = [
              'qualify_bootstrap_root(',
              'qualify_control_plane_subject_freshness(',
              'qualify_operational_policy_context(',
              'qualify_operational_subject_freshness(',
              'authority_state_evidence_plan_provider',
              'plan_control_plane_probes',
              'plan_operational_probe',
              'authority_state_challenge',
              'verify_issued_authority_state_probe',
              'authority_state_source_head_verifier',
              'verify_source_head',
              'authority_state_witness_verifier',
              'verify_witness_evidence',
              'authority_state_transition_verifier',
              'verify_transition_lineage',
              'MAX_CONTROL_PLANE_PROBES',
              'MAX_WITNESSES',
              'MAX_TRUST_BINDINGS',
              'MAX_TRANSITIONS',
              'qualification_time_after_evidence: true',
              'evidence_plan_grants_authority: false',
              'latest_dht_record_authority: false',
              'provider_positive_object_authority: false',
              'probe_authority: false',
              'external_effects_enabled: false',
              'operational: false',
          ]
          for token in required:
              assert token in src, token

          # Freeze causal qualification-time placement after evidence acquisition.
          root = src.split('fn resolve_root(', 1)[1].split('fn resolve_operational_policies', 1)[0]
          assert root.index('let bundle: BootstrapRootCandidateBundle = call_local(') < root.index('let now = now_ms()?;') < root.index('qualify_bootstrap_root(')

          control = src.split('fn resolve_control_plane_freshness(', 1)[1].split('/// Resolve current freshness', 1)[0]
          assert control.index('let evidence = resolve_evidence(') < control.index('let now = now_ms()?;') < control.index('qualify_control_plane_subject_freshness(')

          operational = src.split('pub fn resolve_current_operational_freshness(', 1)[1].split('/// Declarative only.', 1)[0]
          assert operational.index('let policies = resolve_operational_policies(') < operational.index('let context_now = now_ms()?;') < operational.index('qualify_operational_policy_context(')
          assert operational.index('let evidence = resolve_evidence(') < operational.index('let current_now = now_ms()?;') < operational.index('qualify_operational_subject_freshness(')
          assert 'freshness.validate_at(current_now)' in operational

          # One omnibus evidence-oracle provider must not reappear.
          for forbidden in [
              'authority_control_plane_evidence_provider',
              'authority_operational_evidence_provider',
              'get_links(',
              'get_details(',
              'create_entry(',
              'update_entry(',
              'delete_entry(',
              'project_authority_state_as_of',
              'execute_actions',
              'phi',
              'reputation',
              'model_score',
          ]:
              assert forbidden.lower() not in src.lower(), forbidden

          assert '"zomes/authority-current-freshness-verifier/coordinator"' in cargo
          assert 'name: authority_current_freshness_verifier' not in dna
          assert 'discovery and verification are different roles' in inv
          assert 'source-head verification independently reconstructs probe validity' in inv
          assert 'qualification time follows evidence production' in inv
          assert 'witness/trust verification is independent' in inv
          assert 'transition verification is independent' in inv
          assert 'a valid transition prefix is not current authority' in inv
          assert 'deliberately unprovisioned' in inv
          assert 'no latest-record heuristics' in inv
          print('PASS: current freshness preserves probe-action ABI and samples qualification time after evidence production')
          PY
