name: SSF Canonical History Head Wire

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-wire/**'
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-replay-wire-tags/**'
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/**'
      - '.github/workflows/ssf-canonical-history-head-wire.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-wire/**'
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-replay-wire-tags/**'
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/**'
      - '.github/workflows/ssf-canonical-history-head-wire.yml'

permissions:
  contents: read

jobs:
  canonical-history-head-wire:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/Cargo.toml -- --check
      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/Cargo.toml --all-targets
      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/Cargo.toml --lib --target wasm32-unknown-unknown
      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/Cargo.toml
      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/Cargo.toml --all-targets -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-canonical-history-head-wire/Cargo.toml --no-deps
