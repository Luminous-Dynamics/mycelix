name: SSF Actuator Effect Protocol

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-pre-invocation-actuator-qualification/**'
      - 'mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/**'
      - '.github/workflows/ssf-actuator-effect-protocol.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/**'
      - '.github/workflows/ssf-actuator-effect-protocol.yml'

permissions:
  contents: read

jobs:
  actuator-effect-protocol:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/Cargo.toml -- --check
      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/Cargo.toml --all-targets
      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/Cargo.toml --lib --target wasm32-unknown-unknown
      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/Cargo.toml
      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/Cargo.toml --all-targets -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/Cargo.toml --no-deps
