name: SSF Single-Use Issuance Reservation

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/**'
      - 'mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/**'
      - '.github/workflows/ssf-single-use-issuance-reservation.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/**'
      - 'mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/**'
      - '.github/workflows/ssf-single-use-issuance-reservation.yml'

permissions:
  contents: read

jobs:
  reservation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/Cargo.toml -- --check

      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/Cargo.toml --all-targets

      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/Cargo.toml --lib --target wasm32-unknown-unknown

      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/Cargo.toml

      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/Cargo.toml --all-targets -- -D warnings

      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/Cargo.toml --no-deps
