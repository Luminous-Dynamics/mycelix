name: SSF Durable Actuator Invocation Attempt

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-actuator-effect-protocol/**'
      - 'mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/**'
      - '.github/workflows/ssf-durable-actuator-invocation-attempt.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/**'
      - '.github/workflows/ssf-durable-actuator-invocation-attempt.yml'

permissions:
  contents: read

jobs:
  invocation-journal:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/Cargo.toml -- --check
      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/Cargo.toml --all-targets
      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/Cargo.toml --lib --target wasm32-unknown-unknown
      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/Cargo.toml
      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/Cargo.toml --all-targets -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-durable-actuator-invocation-attempt/Cargo.toml --no-deps
