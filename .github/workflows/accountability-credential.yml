name: Accountability Credential

on:
  pull_request:
    paths:
      - "crates/mycelix-accountability-core/**"
      - "crates/mycelix-accountability-verifier/**"
      - "crates/mycelix-accountability-trust/**"
      - "crates/mycelix-accountability-bundle/**"
      - "crates/mycelix-accountability-credential/**"
      - ".github/workflows/accountability-credential.yml"
  push:
    branches:
      - "sif/xenia-release-credential-v0.1"
    paths:
      - "crates/mycelix-accountability-core/**"
      - "crates/mycelix-accountability-verifier/**"
      - "crates/mycelix-accountability-trust/**"
      - "crates/mycelix-accountability-bundle/**"
      - "crates/mycelix-accountability-credential/**"
      - ".github/workflows/accountability-credential.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release-credential:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Format
        run: cargo fmt --check --manifest-path crates/mycelix-accountability-credential/Cargo.toml
      - name: Test
        run: cargo test --manifest-path crates/mycelix-accountability-credential/Cargo.toml
      - name: Clippy
        run: cargo clippy --manifest-path crates/mycelix-accountability-credential/Cargo.toml --all-targets -- -D warnings
