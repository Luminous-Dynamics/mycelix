name: Business Decision Contracts

on:
  pull_request:
    paths:
      - 'crates/mycelix-business-core/**'
      - 'crates/mycelix-business-coordination/**'
      - 'crates/mycelix-business-decision/**'
      - '.github/workflows/business-decision.yml'
  push:
    branches:
      - main
      - 'feat/business-decision-*'
    paths:
      - 'crates/mycelix-business-core/**'
      - 'crates/mycelix-business-coordination/**'
      - 'crates/mycelix-business-decision/**'
      - '.github/workflows/business-decision.yml'

permissions:
  contents: read

jobs:
  business-decision:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Check formatting
        run: cargo fmt --manifest-path crates/mycelix-business-decision/Cargo.toml --all --check
      - name: Clippy
        run: cargo clippy --manifest-path crates/mycelix-business-decision/Cargo.toml --all-targets -- -D warnings
      - name: Test decision contracts
        run: cargo test --manifest-path crates/mycelix-business-decision/Cargo.toml --all-targets
