name: Business Hospitality Shadow Profile

on:
  pull_request:
    paths:
      - 'crates/mycelix-business-core/**'
      - 'crates/mycelix-business-shadow/**'
      - 'crates/mycelix-business-profile-hospitality/**'
      - '.github/workflows/business-hospitality-shadow.yml'
  push:
    branches:
      - main
      - 'feat/business-hospitality-shadow-*'
    paths:
      - 'crates/mycelix-business-core/**'
      - 'crates/mycelix-business-shadow/**'
      - 'crates/mycelix-business-profile-hospitality/**'
      - '.github/workflows/business-hospitality-shadow.yml'

permissions:
  contents: read

jobs:
  hospitality-shadow:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Check formatting
        run: cargo fmt --manifest-path crates/mycelix-business-profile-hospitality/Cargo.toml --all --check
      - name: Clippy
        run: cargo clippy --manifest-path crates/mycelix-business-profile-hospitality/Cargo.toml --all-targets -- -D warnings
      - name: Test hospitality shadow profile
        run: cargo test --manifest-path crates/mycelix-business-profile-hospitality/Cargo.toml --all-targets
