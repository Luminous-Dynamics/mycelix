name: Forge Protected Merge Request

on:
  pull_request:
    paths:
      - 'crates/mycelix-forge-protected-merge-request/**'
      - 'crates/mycelix-forge-merge-protected-review-basis/**'
      - 'crates/mycelix-forge-proposal/**'
      - 'crates/mycelix-forge-repository/**'
      - '.github/workflows/forge-protected-merge-request.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
        with:
          toolchain: 1.96.0
          components: rustfmt, clippy
      - name: rustfmt
        working-directory: crates/mycelix-forge-protected-merge-request
        run: cargo fmt --all -- --check
      - name: clippy
        working-directory: crates/mycelix-forge-protected-merge-request
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: tests
        working-directory: crates/mycelix-forge-protected-merge-request
        run: cargo test --all-features
