name: SSF Remote Path Authorization Coverage

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-contracts/**'
      - 'mycelix-workspace/crates/mycelix-ssf-snapshots/**'
      - 'mycelix-workspace/crates/mycelix-ssf-transitions/**'
      - 'mycelix-workspace/crates/mycelix-ssf-recovery/**'
      - 'mycelix-workspace/crates/mycelix-ssf-authorization/**'
      - 'mycelix-workspace/crates/mycelix-ssf-approved-verifier/**'
      - 'mycelix-workspace/crates/mycelix-ssf-reconciliation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-reconciliation-safety/**'
      - 'mycelix-workspace/crates/mycelix-ssf-authorization-coverage/**'
      - '.github/workflows/ssf-authorization-coverage.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-contracts/**'
      - 'mycelix-workspace/crates/mycelix-ssf-snapshots/**'
      - 'mycelix-workspace/crates/mycelix-ssf-transitions/**'
      - 'mycelix-workspace/crates/mycelix-ssf-recovery/**'
      - 'mycelix-workspace/crates/mycelix-ssf-authorization/**'
      - 'mycelix-workspace/crates/mycelix-ssf-approved-verifier/**'
      - 'mycelix-workspace/crates/mycelix-ssf-reconciliation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-reconciliation-safety/**'
      - 'mycelix-workspace/crates/mycelix-ssf-authorization-coverage/**'
      - '.github/workflows/ssf-authorization-coverage.yml'

permissions:
  contents: read

jobs:
  authorization-coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-authorization-coverage/Cargo.toml -- --check

      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-authorization-coverage/Cargo.toml --all-targets

      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-authorization-coverage/Cargo.toml --lib --target wasm32-unknown-unknown

      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-authorization-coverage/Cargo.toml

      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-authorization-coverage/Cargo.toml --all-targets -- -D warnings

      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-authorization-coverage/Cargo.toml --no-deps
