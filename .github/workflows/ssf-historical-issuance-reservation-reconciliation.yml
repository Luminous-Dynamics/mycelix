name: SSF Historical Issuance Reservation Reconciliation

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/**'
      - '.github/workflows/ssf-historical-issuance-reservation-reconciliation.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-single-use-issuance-reservation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/**'
      - '.github/workflows/ssf-historical-issuance-reservation-reconciliation.yml'

permissions:
  contents: read

jobs:
  historical-reservation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/Cargo.toml -- --check

      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/Cargo.toml --all-targets

      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/Cargo.toml --lib --target wasm32-unknown-unknown

      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/Cargo.toml

      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/Cargo.toml --all-targets -- -D warnings

      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-historical-issuance-reservation-reconciliation/Cargo.toml --no-deps
