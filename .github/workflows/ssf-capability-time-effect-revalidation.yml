name: SSF Capability-Time Effect Revalidation

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-effect-admission-rebind/**'
      - 'mycelix-workspace/crates/mycelix-ssf-execution-surface-admission/**'
      - '.github/workflows/ssf-capability-time-effect-revalidation.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-effect-admission-rebind/**'
      - 'mycelix-workspace/crates/mycelix-ssf-execution-surface-admission/**'
      - '.github/workflows/ssf-capability-time-effect-revalidation.yml'

permissions:
  contents: read

jobs:
  revalidation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/Cargo.toml -- --check
      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/Cargo.toml --all-targets
      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/Cargo.toml --lib --target wasm32-unknown-unknown
      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/Cargo.toml
      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/Cargo.toml --all-targets -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-capability-time-effect-revalidation/Cargo.toml --no-deps
