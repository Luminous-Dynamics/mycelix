name: SSF Current Authority Revalidation

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-authorization/**'
      - 'mycelix-workspace/crates/mycelix-ssf-contracts/**'
      - 'mycelix-workspace/crates/mycelix-ssf-local-adoption-readiness/**'
      - 'mycelix-workspace/crates/mycelix-ssf-reserved-issuance-rebind/**'
      - 'mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/**'
      - '.github/workflows/ssf-current-authority-revalidation.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-authorization/**'
      - 'mycelix-workspace/crates/mycelix-ssf-contracts/**'
      - 'mycelix-workspace/crates/mycelix-ssf-local-adoption-readiness/**'
      - 'mycelix-workspace/crates/mycelix-ssf-reserved-issuance-rebind/**'
      - 'mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/**'
      - '.github/workflows/ssf-current-authority-revalidation.yml'

permissions:
  contents: read

jobs:
  revalidation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/Cargo.toml -- --check

      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/Cargo.toml --all-targets

      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/Cargo.toml --lib --target wasm32-unknown-unknown

      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/Cargo.toml

      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/Cargo.toml --all-targets -- -D warnings

      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-current-authority-revalidation/Cargo.toml --no-deps
