name: SSF Persisted Registration Rebind

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-adoption-authority-candidate/**'
      - 'mycelix-workspace/crates/mycelix-ssf-adoption-authority-registration/**'
      - 'mycelix-workspace/crates/mycelix-ssf-historical-registration-reconciliation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/**'
      - '.github/workflows/ssf-persisted-registration-rebind.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-adoption-authority-candidate/**'
      - 'mycelix-workspace/crates/mycelix-ssf-adoption-authority-registration/**'
      - 'mycelix-workspace/crates/mycelix-ssf-historical-registration-reconciliation/**'
      - 'mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/**'
      - '.github/workflows/ssf-persisted-registration-rebind.yml'

permissions:
  contents: read

jobs:
  rebind:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/Cargo.toml -- --check

      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/Cargo.toml --all-targets

      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/Cargo.toml --lib --target wasm32-unknown-unknown

      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/Cargo.toml

      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/Cargo.toml --all-targets -- -D warnings

      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-persisted-registration-rebind/Cargo.toml --no-deps
