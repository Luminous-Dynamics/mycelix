name: SSF No Third Attempt Provenance Gate

on:
  pull_request:
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-canonical-completed-outcome-history-audit/**'
      - 'mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/**'
      - '.github/workflows/ssf-no-third-attempt-provenance-gate.yml'
  push:
    branches: [main]
    paths:
      - 'mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/**'
      - '.github/workflows/ssf-no-third-attempt-provenance-gate.yml'

permissions:
  contents: read

jobs:
  no-third-attempt-provenance-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown
      - name: Format
        run: cargo fmt --manifest-path mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/Cargo.toml -- --check
      - name: Native check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/Cargo.toml --all-targets
      - name: no_std WASM check
        run: cargo check --manifest-path mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/Cargo.toml --lib --target wasm32-unknown-unknown
      - name: Tests
        run: cargo test --manifest-path mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/Cargo.toml
      - name: Strict Clippy
        run: cargo clippy --manifest-path mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/Cargo.toml --all-targets -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --manifest-path mycelix-workspace/crates/mycelix-ssf-no-third-attempt-provenance-gate/Cargo.toml --no-deps
