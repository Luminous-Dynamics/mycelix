[package]
name = "election-transparency-witness"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Append-only election checkpoint and independent witness contracts for Mycelix public elections"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
election-integrity-types = { path = "../election-integrity-types" }

[workspace]
