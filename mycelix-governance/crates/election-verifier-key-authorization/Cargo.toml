[package]
name = "election-verifier-key-authorization"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Immutable verifier-key authorization roots for Mycelix public elections"

[lib]
crate-type = ["rlib"]

[dependencies]
election-integrity-types = { path = "../election-integrity-types" }
sha2 = "0.10"

[workspace]
