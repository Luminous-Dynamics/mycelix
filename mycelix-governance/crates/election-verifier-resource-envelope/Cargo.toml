[package]
name = "election-verifier-resource-envelope"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Bounded resource preflight contracts for Mycelix public-election offline verification"

[lib]
crate-type = ["rlib"]

[dependencies]
election-integrity-types = { path = "../election-integrity-types" }
election-verifier-contract = { path = "../election-verifier-contract" }

[workspace]
